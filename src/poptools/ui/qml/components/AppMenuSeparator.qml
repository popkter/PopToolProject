import QtQuick
import QtQuick.Controls
import "../theme"

MenuSeparator {
    implicitHeight: 9
    contentItem: Rectangle {
        y: 4
        height: 1
        color: Theme.outlineVariant
    }
}
